#include <bits/stdc++.h>
using namespace std;

#define fastio ios::sync_with_stdio(false); cin.tie(NULL)

int main() {
    fastio;

    int t;
    cin >> t;
    while (t--) {
        long long x,y;
        cin >> y >> x;

        long long k = max(y,x);
        long long k2 = k*k;

        if(k%2 == 0){
            
        }

    }

    return 0;
}
